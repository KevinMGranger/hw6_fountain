//class Surface {
//  ArrayList<ArrayList<Vec2>> surfaces = new ArrayList<ArrayList<Vec2>>();
//
//  //       for (; (theta <= (TWO_PI + (HALF_PI/2))) && (x < width-FTN_EDGE_OFFSET); theta += (HALF_PI/16), x++) {
//
////  Surface(int numsurfaces, float... breakpoints) {
////    if ( numsurfaces != (breakpoints.length*2) )
////    {
////      throw new RuntimeException("d'oh");
////    }
////
////    float x = FTN_EDGE_OFFSET;
////    float xEnd = width-FTN_EDGE_OFFSET;
////   
////
////    for (int i = 0; i < numsurfaces; i++) {
////      surfaces.add(new ArrayList<Vec2>());
////
////      float theta = breakpoints[i*2];
////      float thetaEnd = breakpoints[i*2+1];
////      float thetaInc = 
////
////      for (; theta <= thetaEnd; theta += thetainc, x += xinc) {
////        float y = map(sin(theta), -1, 1, -height/8, height/8);
////        surfaces.get(i).add(new Vec2(x, y));
////      }
////    }
////  }
//
//
//}
